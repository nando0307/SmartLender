version: '3.8'

services:
  smartlend:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
      - ./models:/app/models
      - ./results:/app/results
    environment:
      - STREAMLIT_SERVER_HEADLESS=true
